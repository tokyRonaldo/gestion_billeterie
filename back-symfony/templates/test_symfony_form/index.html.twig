{% extends 'base.html.twig' %}

{% block title %}Hello TestSymfonyController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <h1>Apropos index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Name</th>
                <th>Description</th>
                <th>Logo</th>
                <th>Image</th>
                <th>PhonDescription</th>
                <th>Logo<eDescription</th>
                <th>Logo<Number</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for event in evens%}
            <tr>
            <td>{{event.id}}</td>
            <td>{{event.name}}</td>
            <td>{{event.type}}</td>
            <td>{{event.description}}</td>
            <td><a href="{{path('app_test_symfony_edit',{'id',event.id})}}">edit</a></td>
            <td><a href="{{path('app_test_symfony_delete',{'id',event.id})}}">edit</a></td>
            <td>{{event.description}}</td>
            </tr>
        {% endfor %}
        </tbody>
        </table>

{% endblock %}
