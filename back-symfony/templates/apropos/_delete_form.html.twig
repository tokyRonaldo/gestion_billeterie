<form method="post" action="{{ path('app_apropos_delete', {'id': apropo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apropo.id) }}">
    <button class="btn">Delete</button>
</form>
